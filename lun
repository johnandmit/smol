#include <stdio.h>
#include<math.h>

int main(void) 
{
  int n;
  printf("nhap so nguyen duong: ");
  scanf("%i",&n);
  for(size_t b=floor(sqrt(n));b>1;b--){
    if(n%b==0){
      printf("n knong phai la so nguyen to");
      return 0;
    }
  }
  printf("n la so nguyen to");
}
